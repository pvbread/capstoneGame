from .. import BaseItem
import random

class Primordial_badge(BaseItem.BaseItem):
    def __init__(self, name, tempS, swiftness, healNum, statEffect):
        super().__init__(self, name, tempS, swiftness, healNum, statEffect)

    